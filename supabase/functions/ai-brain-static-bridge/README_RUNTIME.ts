// Runtime source is deployed to Supabase as ai-brain-static-bridge.
// This repository copy intentionally omits the browser token value.
// Purpose:
// 1. Bridge the current gh-pages demo UI to the private ai-brain storage.
// 2. Accept only bounded text/PDF/image sources.
// 3. Process PDF text and image context server-side.
// 4. Be removed when the static demo is replaced by the authenticated Vite frontend.
export {};
