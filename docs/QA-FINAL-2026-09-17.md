# Hárpia Patrimonial — QA final integrada

Data: 17/09/2026
Branch/base: `frente-01`
Base inicial auditada: `8a45920fef7459e38a810d3f633c53ed33e65be6`

## Resultado geral

🟠 **QA FINAL PARCIAL / EM ANDAMENTO**.

A camada integrada de backend, dados, autorização e automação foi validada com testes reais no projeto Supabase da Hárpia. Build/typecheck com Node 24 e E2E visual em navegador ainda permanecem `NÃO VERIFICADO` neste ambiente e, portanto, a base não deve ser marcada como `INTEGRADA/VERDE` ainda.

## Ambiente verificado

- Supabase `Harpia Patrimonial`: `ACTIVE_HEALTHY`.
- Postgres 17.6.1, região `sa-east-1`.
- Security Advisor: `0` lints.
- Performance Advisor: somente 5 avisos `INFO` de índices ainda não utilizados em base vazia.
- Edge Functions principais ativas, incluindo `admin-user`, `public-lead-ingest`, `client-area-data`, `automation-event-worker` v4, `f05-runtime-worker`, `f05-delay-worker`, `ai-model-invoke`, `ai-credential-vault` e `public-organization-contact`.
- Scheduler `f05-delay-resume-30s` ativo.
- Realtime publicado para `catalog_items`, `platform_module_state` e `f05_shared_storage`.
- Bucket `catalog-media` existente, público, limite de 50 MB e MIME types previstos.

## Testes reais executados

### Auth / RBAC / RLS

🟢 **COMPLETO E TESTÁVEL NO BACKEND**

Teste transacional, sem resíduos:

- administrador temporário: 22 permissões efetivas;
- viewer temporário: 11 permissões `*.view` e 0 permissões de gestão;
- cliente final: 0 grupos internos visíveis por RLS;
- usuário interno não consegue alterar o próprio `is_active/account_type`;
- base voltou a `auth.users=0` após o teste.

### Catálogo

🟢 **COMPLETO E TESTÁVEL NO BACKEND**

Teste transacional, sem resíduos:

- `draft -> paused` rejeitado;
- empreendimento não pode ser vendido com unidade ativa não vendida;
- empreendimento e unidade publicam com `published_at`;
- ao pausar o empreendimento, a unidade deixa de ser elegível publicamente;
- catálogo permaneceu vazio após rollback/limpeza.

### Captação pública / CRM / outbox

🟢 **COMPLETO E TESTÁVEL NO BACKEND/EDGE**

Teste pelo endpoint real `public-lead-ingest`:

- resposta HTTP 201;
- lead criado no estado CRM;
- evento `lead.created` criado no outbox;
- worker processou em uma tentativa;
- resposta confirmou `automaticMessageSent=false`;
- lead/histórico/outbox removidos após o teste.

Ações CRM também validadas transacionalmente:

- `move_stage`;
- `add_tag`;
- `update_field`;
- `create_task`;
- eventos derivados criados somente quando houve mudança real.

### SalesBot / Automatize / runtime F05

🟢 **COMPLETO E TESTÁVEL NO BACKEND/EDGE**

Testes reais:

1. Integridade de grafo:
   - bot ativo válido aceito;
   - ciclo `A -> B -> A` rejeitado pelo trigger de integridade.

2. E2E `CRM -> Automatize -> SalesBot`:
   - lead real temporário criado via Edge Function;
   - automação `lead.created` executada;
   - ação `start_salesbot` aceita;
   - `f05-runtime-worker` criou execução;
   - execução terminou com `completed`.

3. Delay durável:
   - bot com `delay 1s` iniciado;
   - execução foi retomada pelo scheduler/worker;
   - execução terminou em `completed`.

4. Mensagem sem canal real:
   - bloco `message` retornou `not_configured`;
   - execução ficou `paused` com `resumeMode=retry_current`;
   - evento ficou em retry/falha operacional;
   - nenhuma mensagem foi marcada como enviada.

Todos os SalesBots, automações, execuções e leads temporários foram removidos. Estado final confirmado em zero.

### Cofre / scheduler / segurança operacional

🟢 **COMPLETO E TESTÁVEL NO BACKEND**

- token do scheduler existe no Vault;
- hash do token confere com `private.f05_scheduler_auth`;
- job cron ativo;
- RLS habilitado nas tabelas públicas operacionais;
- policies de Storage contemplam SELECT/INSERT/UPDATE/DELETE para `catalog.manage`;
- `service_role` permanece server-side.

### Contato público

🟢 **COMPLETO E TESTÁVEL NO EDGE**

- `public-organization-contact` respondeu HTTP 200;
- retorno atual: `phone=null`;
- comportamento está correto: não inventar número enquanto o contato real não foi configurado.

## Estado final após QA

Confirmado após limpeza:

- `auth.users = 0`;
- `catalog_items = 0`;
- CRM leads = 0;
- CRM history = 0;
- `automation_event_outbox = 0`;
- `automation_action_runs = 0`;
- SalesBots = 0;
- automações = 0;
- execuções SalesBot = 0.

Nenhum dado fictício/temporário de QA ficou na operação.

## NÃO VERIFICADO neste ambiente

🔴 **PENDENTE DE AMBIENTE COMPATÍVEL / NAVEGADOR**

- `npm run typecheck` com Node `>=24 <25`;
- `npm run build` com Node `>=24 <25`;
- instalação limpa de dependências;
- E2E visual autenticado admin/viewer/cliente no navegador;
- sessão após fechar/reabrir navegador;
- confirmação/recovery de e-mail real e redirects finais;
- upload real de mídia pela UI;
- Realtime observado em duas sessões simultâneas;
- responsividade desktop/mobile e validação visual final;
- console do navegador sem erros relevantes.

Motivo objetivo: o ambiente disponível nesta QA possui Node `v22.16.0` e o checkout direto do GitHub falhou por indisponibilidade de DNS externo. O projeto exige Node `>=24 <25`. Esses testes não foram simulados nem marcados como aprovados.

## Observação de reprodutibilidade

`package.json` usa versões diretas fixadas, porém não há `package-lock.json` versionado na `frente-01`. Gerar e versionar o lockfile deve ser considerado no ambiente Node 24 quando a instalação limpa for executada.

## Próximo passo obrigatório para verde final

Executar, em ambiente com Node 24 e navegador:

1. `npm ci` ou instalação equivalente após gerar/confirmar lockfile;
2. `npm run typecheck`;
3. `npm run build`;
4. E2E visual/auth/session/upload/realtime em duas sessões;
5. registrar resultados e somente então marcar a base como `INTEGRADA/VERDE`.
