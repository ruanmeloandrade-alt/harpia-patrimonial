# Frente 06.4, 06.5 e 06.6 — UX operacional

Data: 24/09/2026  
Branch: `work/f06-ux-operacao-20260924`

## Escopo assumido

### F06.4 — CRM

Status: 🟡 IMPLEMENTADO EM CÓDIGO / QA VISUAL EM NAVEGADOR NÃO VERIFICADO

Entregue:

- mantém múltiplos funis configuráveis e troca entre funis;
- mantém Kanban por etapas e movimentação de leads;
- adiciona resumo do funil selecionado com status, quantidade de etapas e leads reais;
- adiciona entrada `Automatize` no topo do funil, apontando para `/interno/automatize`;
- adiciona duplicação de funil;
- duplicação copia somente a estrutura de etapas e nunca copia leads;
- duplicação foi movida para `CrmService`, evitando a implementação por várias operações independentes na UI;
- nenhuma mensagem é disparada automaticamente ao criar lead;
- sem mock permanente.

Arquivos principais:

- `src/features/crm/CrmWorkspaceCore.tsx`
- `src/features/crm/crm.module.css`
- `src/features/crm/serviceCore.ts`

Commits principais:

- `3d1c8f78a821863cb876d45226573adc94e6d356`
- `264e228dd23981f032d21ba32bbb8dfe81ef85ab`
- `961e486f94d60f243529505c3af0f06054dcaa70`
- `ff10f6c35d00036b7498d1bd8133a872479dca3f`

## F06.5 — Inbox

Status: 🟡 IMPLEMENTADO EM CÓDIGO / DEPENDÊNCIA DE TRANSPORTE PARA GRUPOS E FORMULÁRIOS

Entregue:

- preserva layout operacional em três áreas: conversas, chat e contexto CRM;
- mantém chat, composer e contexto visíveis mesmo quando não existem conversas;
- exibe estado operacional explícito: Atendimento humano, SalesBot ativo, Agente IA ativo ou automação pausada;
- traz ações rápidas de SalesBot e Agente IA para a área do chat usando os contratos reais já existentes;
- preserva envio real de texto e mídia somente quando o transporte está conectado;
- preserva áudio, imagem, vídeo e documento através do fluxo real de mídia;
- mantém ações de CRM: etapa, responsável, tags, campos personalizados e próxima ação;
- `Formulário` e `Criar grupo` ficam visíveis, mas não simulam sucesso: a UI informa que o transporte WhatsApp atual não expõe essas operações;
- corrige o layout do chat para suportar a nova barra rápida sem deslocar o histórico;
- sem conversa fictícia ou mensagem fake.

Arquivos principais:

- `src/features/inbox/InboxWorkspaceCore.tsx`
- `src/features/inbox/inbox.module.css`

Commits principais:

- `88f80446bbbf14112bac216c885da2d8d248fa30`
- `e66feee33c4cdf51e5e2fe96ca6ecdb25ef8637d`
- `bcf1af354fe6bee19a34a15568575002d8bffbe7`

Dependência explícita:

- criação real de grupo e envio de formulário dependem de suporte no transporte WhatsApp/Frente 08. Não foi criado comportamento falso na Frente 06.

## F06.6 — Dashboard

Status: 🟡 IMPLEMENTADO NA BRANCH / QA VISUAL EM NAVEGADOR NÃO VERIFICADO

Estado atual validado por inspeção da branch:

- painel permanece visualmente completo durante atualização e quando os dados estão zerados;
- erro de atualização não desmonta a tela;
- cards continuam visíveis com zero quando não existe operação real;
- resumo comercial e catálogo permanecem estruturados;
- funil comercial visual usa métricas reais;
- distribuição por região e origem usa somente dados presentes nas fontes;
- estados vazios têm representação visual própria;
- não há preenchimento com dado demonstrativo.

Arquivos principais:

- `src/features/dashboard/DashboardPage.tsx`
- `src/features/dashboard/dashboard.css`

Commits existentes na branch que compõem esta atualização visual:

- `56cf2de728560ec424f99a449fa39ee0beb5bb71`
- `bdb80bf409fe50569ec518c880b5720e44adf797`

## F06.7 Catálogo de produtos e vínculo com clientes

Status: 🟡 IMPLEMENTADO EM CÓDIGO E BANCO / SMOKE VISUAL E E2E AINDA NÃO VERIFICADOS

Entregue:

- menu interno renomeado de `Imóveis` para `Produtos`;
- catálogo interno generalizado para três categorias: imóvel, produto e serviço;
- cadastro com código, nome, descrição, preço, desconto e tags;
- desconto suporta percentual ou valor fixo, com validação no frontend e no banco;
- campos imobiliários continuam disponíveis somente para itens do tipo imóvel;
- catálogo público de imóveis continua exibindo somente imóveis publicados;
- criada a relação persistente `crm_lead_products` entre cliente/lead e item do catálogo;
- ficha do cliente no CRM recebe o bloco `Produtos do cliente`;
- vínculo suporta Interesse, Orçamento e Comprado;
- vínculo preserva quantidade, preço negociado, desconto e observação comercial;
- produto associado pode ter seu status comercial alterado ou ser removido da ficha;
- RLS aplicada à relação cliente x produto;
- Security Advisor do Supabase retornou 0 lints após a migration;
- nenhuma linha fictícia foi inserida para simular produto, cliente ou compra.

Arquivos principais:

- `src/features/catalog/CatalogAdminPage.tsx`
- `src/features/catalog/types.ts`
- `src/features/catalog/catalogRepository.ts`
- `src/features/catalog/supabaseCatalogRepository.ts`
- `src/features/catalog/publicCatalog.ts`
- `src/features/crm/LeadProductsPanel.tsx`
- `src/features/crm/leadProductRepository.ts`
- `src/features/crm/CrmWorkspace.tsx`
- `src/features/crm/CrmWorkspaceCore.tsx`
- `src/shared/components/InternalShell.tsx`
- `supabase/schema/product_catalog_and_crm_associations.sql`

Banco aplicado:

- `catalog_items.item_type`;
- `catalog_items.discount_type`;
- `catalog_items.discount_value`;
- `catalog_items.tags`;
- `crm_lead_products` com chave composta por cliente e item do catálogo.

Pendente para ficar verde:

- executar `npm run typecheck`;
- executar `npm run build`;
- cadastrar um produto real e vinculá-lo a um cliente real;
- validar o fluxo na interface em navegador.

## QA executada nesta rodada

- releitura da branch antes das alterações;
- conferência dos contratos reais de CRM, Inbox e transporte WhatsApp;
- conferência de que `/interno/automatize` existe no roteador;
- conferência de que o transporte atual aceita texto e mídia, mas não expõe grupo/formulário;
- inspeção estática final dos pontos alterados;
- correção de conflito de layout encontrado no Inbox após a inclusão da barra de ações.

### NÃO VERIFICADO

- `npm run typecheck`;
- `npm run build`;
- navegação visual em navegador desktop/mobile;
- teste real com canal WhatsApp conectado;
- teste visual final pelo usuário.

Motivo: esta execução está operando o repositório pelo conector do GitHub e não possui um checkout executável do projeto no runtime local. Nenhum sucesso de build foi presumido.

## Handoff

A Frente 06.4/06.5/06.6 permanece isolada na branch `work/f06-ux-operacao-20260924`.

Não fazer merge forçado para `main` antes da rodada de integração/QA visual das frentes independentes.

Próxima ação recomendada para a integração: executar typecheck/build, abrir CRM, Inbox e Dashboard em desktop/mobile e validar visualmente os estados vazio, zerado, com funil e com conversa real.
